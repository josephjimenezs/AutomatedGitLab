GITLAB_EXTERNAL_URL=http://xotrik-gitlab
GITLAB_CLONE_URL=http://xotrik-gitlab:80
TUNNEL_TOKEN=

# GITLAB_ROOT_PASSWORD is intentionally NOT set here.
# It is provided at runtime by start.ps1 (prompted or via -RootPassword),
# so it never lives in a file on disk.
